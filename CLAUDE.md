# Agent Configuration & Build Guide

coactl local web app: **Global vs Project** modes → Tools → Skills → manage/import.

Product requirements: [docs/PRD.md](docs/PRD.md)

```bash
npm install
npm run build -w @coactl/domain
npm run build -w @coactl/server
npm run dev:server
npm run dev:web
```

Legacy CLI: tag `cli-v0.1.0`, branch `archive/cli-v0.1`.
